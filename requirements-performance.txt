# Optional performance enhancements for Windows
# Install with: pip install -r requirements-performance.txt

# High-performance asyncio event loop for Windows (5x faster than default)
winloop>=0.2.2

# Alternative: uvloop for Unix systems (not needed on Windows)
# uvloop>=0.19.0; sys_platform != "win32"